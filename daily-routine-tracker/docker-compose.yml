services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - JWT_SECRET=${JWT_SECRET:-change-me-to-a-random-string}
      - DATA_DIR=/app/data
      - PORT=3000
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
